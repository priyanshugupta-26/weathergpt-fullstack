import{api,state,citySelector}from'./common.js';await citySelector(citySelect);document.querySelectorAll('[data-domain]').forEach(b=>b.onclick=async()=>{const d=await api(`/api/advisory?domain=${b.dataset.domain}&lat=${state.lat}&lon=${state.lon}`);domainName.textContent=b.dataset.domain;adviceTitle.textContent=`${b.dataset.domain[0].toUpperCase()+b.dataset.domain.slice(1)} advisory for ${state.city}`;advice.innerHTML=d.advice.map(x=>`<div class="card" style="padding:14px">${x}</div>`).join('')});
