import {api,state,citySelector,number} from './common.js';await citySelector(citySelect);
function render(d){alertList.innerHTML=d.alerts.map(a=>`<div class="card alert ${a.severity}"><div class="kicker">${a.severity} severity</div><h3>${a.type}</h3><p class="muted">${a.message}</p></div>`).join('');const w=d.weather;signal.innerHTML=`<div><span class="muted">Wind gust</span><div class="metric-big">${number(w.wind_gusts_10m,' km/h')}</div></div><div><span class="muted">Rain</span><div>${number(w.precipitation,' mm')}</div></div><div><span class="muted">Temperature</span><div>${number(w.temperature_2m,'°C')}</div></div>`}
render(await api(`/api/alerts?lat=${state.lat}&lon=${state.lon}`));
const ws=new WebSocket(`${location.protocol==='https:'?'wss':'ws'}://${location.host}/ws/alerts`);ws.onmessage=e=>render(JSON.parse(e.data));refresh.onclick=()=>ws.send(JSON.stringify({lat:state.lat,lon:state.lon}));
notifyBtn.onclick=async()=>{if(!('Notification'in window))return alert('Notifications unsupported');const p=await Notification.requestPermission();if(p==='granted')new Notification('WeatherGPT alerts enabled',{body:`Monitoring ${state.city}. Production version will use service worker push.`});};
